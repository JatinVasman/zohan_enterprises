# Resend — server-side only (used by /api/enquiry serverless function)
# Get your API key at https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here

# Optional: override the FROM address (must be on a Resend-verified domain)
# If omitted, defaults to onboarding@resend.dev (works without domain verification)
# RESEND_FROM_EMAIL=enquiry@zohanenterprise.com
